import { Box } from '@mui/material';

import ListOfCategories from '../features/category/components/ListOfCategories';

const HomePage = () => {
  return (
    <Box>
      <ListOfCategories />
    </Box>
  );
};

export default HomePage;
